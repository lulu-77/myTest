node {
    stage('build-using-scm'){
     echo 'build';
        mvn clean install -DskipTests=ture;
    }
    
    stage('build-using-scm'){
     echo 'build';
        mvn clean install -DskipTests=ture;
    }
    
    stage('test-using-scm'){
     echo 'test';
    }
    
    stage('deploy-using-scm'){
     echo 'deploy';
    }
}
